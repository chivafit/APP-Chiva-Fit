[1mdiff --git a/app.js b/app.js[m
[1mindex 662759d..bc8d55c 100644[m
[1m--- a/app.js[m
[1m+++ b/app.js[m
[36m@@ -9,7 +9,7 @@[m [mimport {[m
   copyWhatsAppMessageForCustomer as copyWhatsAppMessageForCustomerImpl,[m
   openWhatsAppForCustomer as openWhatsAppForCustomerImpl[m
 } from "./ia.js?v=20260316-6";[m
[31m-import { escapeHTML, safeJsonParse, escapeJsSingleQuote, safeSetItem, debounce, withRetry } from "./utils.js?v=20260317-3";[m
[32m+[m[32mimport { escapeHTML, safeJsonParse, escapeJsSingleQuote, safeSetItem, debounce, withRetry, parseDateToIso, fmtDateBrFromIso } from "./utils.js?v=20260317-5";[m
 import { initSentry, captureError, captureMessage, setSentryUser } from "./sentry.js?v=20260317-3";[m
 import { CRMStore } from "./store.js?v=20260316-6";[m
 import { STORAGE_KEYS } from "./constants.js?v=20260316-6";[m
[36m@@ -33,7 +33,7 @@[m [mimport {[m
   syncBling as syncBlingImpl,[m
   syncBlingProdutos as syncBlingProdutosImpl,[m
   backfillBlingEnderecos as backfillBlingEnderecosImpl[m
[31m-} from "./sync/bling.js?v=20260316-6";[m
[32m+[m[32m} from "./sync/bling.js?v=20260317-4";[m
 import {[m
   syncYampi as syncYampiImpl,[m
   syncCarrinhosAbandonadosYampi as syncCarrinhosAbandonadosYampiImpl,[m
[36m@@ -177,6 +177,9 @@[m [mlet allCustomers = [];[m
 let customerIntel = [];[m
 let customerIntelligence = [];[m
 let activeCh = "all";[m
[32m+[m[32mlet savedFilters = safeJsonParse("crm_filtros_salvos", []);[m
[32m+[m[32mlet selectedClientes = new Set();[m
[32m+[m[32mlet selectedCarrinhos = new Set();[m
 let charts   = {};[m
 let activeSegment = null;[m
 let syncTimer = null;[m
[36m@@ -413,7 +416,7 @@[m [mfunction checkEstoqueCritico(){[m
         tipo: "estoque_critico",[m
         conteudo: { itens: crit.map(i=>({ id: i?.id, nome: i?.nome, estoque_atual: i?.estoque_atual ?? i?.estoque ?? 0, estoque_minimo: i?.estoque_minimo ?? i?.minimo ?? 0 })) },[m
         created_at: new Date().toISOString()[m
[31m-      }).then(()=>{}).catch(()=>{});[m
[32m+[m[32m      }).then(()=>{}).catch(e=>{ console.warn("[alertas] falha ao inserir alerta de estoque crítico:", e?.message || String(e)); captureError(e, { context: "insert_alerta_estoque" }); });[m
     }[m
   }[m
 }[m
[36m@@ -720,6 +723,320 @@[m [mfunction enterApp(userEmail){[m
   })();[m
 }[m
 window.enterApp = enterApp;[m
[32m+[m[32mwindow.saveCurrentFilter = saveCurrentFilter;[m
[32m+[m[32mwindow.applyFilter = applyFilter;[m
[32m+[m[32mwindow.deleteSavedFilter = deleteSavedFilter;[m
[32m+[m[32mwindow.toggleClienteSelection = toggleClienteSelection;[m
[32m+[m[32mwindow.toggleSelectAll = toggleSelectAll;[m
[32m+[m[32mwindow.clearSelection = clearSelection;[m
[32m+[m[32mwindow.bulkExportCSV = bulkExportCSV;[m
[32m+[m[32mwindow.bulkCopyWhatsApp = bulkCopyWhatsApp;[m
[32m+[m[32mwindow.bulkMarkContacted = bulkMarkContacted;[m
[32m+[m
[32m+[m[32m/* ═══════════════════════════════════════════════════[m
[32m+[m[32m   C — ALERTAS DE CARRINHOS QUENTES EM TEMPO REAL[m
[32m+[m[32m═══════════════════════════════════════════════════ */[m
[32m+[m[32mconst CARRINHO_ALERTA_KEY = "crm_carrinhos_alertados_ids";[m
[32m+[m[32mconst CARRINHO_HOT_MIN = 20; // janela em minutos considerada "quente"[m
[32m+[m
[32m+[m[32mfunction getCarrinhosAlertados(){[m
[32m+[m[32m  try{ return new Set(JSON.parse(localStorage.getItem(CARRINHO_ALERTA_KEY)||"[]")); }catch(_){ return new Set(); }[m
[32m+[m[32m}[m
[32m+[m[32mfunction setCarrinhosAlertados(s){[m
[32m+[m[32m  try{ localStorage.setItem(CARRINHO_ALERTA_KEY, JSON.stringify([...s].slice(-300))); }catch(_){}[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mfunction checkCarrinhosQuentes(){[m
[32m+[m[32m  const lista = (carrinhosAbandonados||[]).map(normalizeCarrinhoAbandonado).filter(c=>c.checkout_id);[m
[32m+[m[32m  const quentes = lista.filter(c=>!c.recuperado && !c.perdido && minutosDesdeIso(c.criado_em) != null && minutosDesdeIso(c.criado_em) < CARRINHO_HOT_MIN);[m
[32m+[m
[32m+[m[32m  // Atualizar badge nav[m
[32m+[m[32m  const badge = document.getElementById("badge-comercial");[m
[32m+[m[32m  if(badge){[m
[32m+[m[32m    if(quentes.length){[m
[32m+[m[32m      badge.textContent = String(quentes.length);[m
[32m+[m[32m      badge.style.display = "";[m
[32m+[m[32m      badge.classList.add("hot");[m
[32m+[m[32m    } else {[m
[32m+[m[32m      badge.style.display = "none";[m
[32m+[m[32m      badge.classList.remove("hot");[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  // Alertar apenas carrinhos ainda não avisados[m
[32m+[m[32m  const jaAlertados = getCarrinhosAlertados();[m
[32m+[m[32m  const novos = quentes.filter(c=>!jaAlertados.has(String(c.checkout_id)));[m
[32m+[m[32m  if(!novos.length) return;[m
[32m+[m
[32m+[m[32m  novos.forEach(c=>jaAlertados.add(String(c.checkout_id)));[m
[32m+[m[32m  setCarrinhosAlertados(jaAlertados);[m
[32m+[m
[32m+[m[32m  const nomes = novos.slice(0,3).map(c=>String(c.cliente_nome||"").split(" ")[0]||"Cliente").join(", ");[m
[32m+[m[32m  const extras = novos.length > 3 ? ` +${novos.length-3}` : "";[m
[32m+[m[32m  const plural = novos.length>1 ? "carrinhos quentes" : "carrinho quente";[m
[32m+[m[32m  toast(`🔥 ${novos.length} novo${novos.length>1?"s":""} ${plural}: ${nomes}${extras}`, "warning");[m
[32m+[m
[32m+[m[32m  // Tenta navegação via Notification API como fallback silencioso[m
[32m+[m[32m  try{[m
[32m+[m[32m    if("Notification" in window && Notification.permission === "granted"){[m
[32m+[m[32m      new Notification("🔥 Carrinho quente — Chiva Fit", {[m
[32m+[m[32m        body: `${novos.length} novo${novos.length>1?"s":""} ${plural} nos últimos ${CARRINHO_HOT_MIN} min.`,[m
[32m+[m[32m        silent: true[m
[32m+[m[32m      });[m
[32m+[m[32m    }[m
[32m+[m[32m  }catch(_){}[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m// Exposto globalmente para ser chamado após sync[m
[32m+[m[32mwindow.checkCarrinhosQuentes = checkCarrinhosQuentes;[m
[32m+[m
[32m+[m[32m/* ═══════════════════════════════════════════════════[m
[32m+[m[32m   I — EXPORTAR CSV DE CARRINHOS ABANDONADOS[m
[32m+[m[32m═══════════════════════════════════════════════════ */[m
[32m+[m[32mfunction exportCarrinhosCSV(){[m
[32m+[m[32m  const lookup = buildClienteLookupParaCarrinhos();[m
[32m+[m[32m  const q  = String((document.getElementById("car-search")||{}).value||"").trim().toLowerCase();[m
[32m+[m[32m  const st = String((document.getElementById("car-status")||{}).value||"");[m
[32m+[m[32m  const resp = String((document.getElementById("car-responsavel")||{}).value||"");[m
[32m+[m
[32m+[m[32m  const list = (carrinhosAbandonados||[]).map(normalizeCarrinhoAbandonado).filter(c=>{[m
[32m+[m[32m    if(!c.checkout_id) return false;[m
[32m+[m[32m    if(st==="abertos"    && (c.recuperado||c.perdido)) return false;[m
[32m+[m[32m    if(st==="recuperados" && !c.recuperado) return false;[m
[32m+[m[32m    if(st==="perdidos"   && !c.perdido) return false;[m
[32m+[m[32m    if(st==="quentes"    && (c.recuperado||c.perdido||(minutosDesdeIso(c.criado_em)==null||minutosDesdeIso(c.criado_em)>=120))) return false;[m
[32m+[m[32m    if(resp && (c.responsavel||"")!==resp) return false;[m
[32m+[m[32m    if(q){[m
[32m+[m[32m      const hit=String(c.cliente_nome||"").toLowerCase().includes(q)||String(c.email||"").toLowerCase().includes(q)||rawPhone(c.telefone||"").includes(rawPhone(q));[m
[32m+[m[32m      if(!hit) return false;[m
[32m+[m[32m    }[m
[32m+[m[32m    return true;[m
[32m+[m[32m  }).map(c=>{[m
[32m+[m[32m    const calc = calcularScoreRecuperacaoCarrinho(c, lookup);[m
[32m+[m[32m    const score = c.score_recuperacao==null ? calc.score : Number(c.score_recuperacao||0);[m
[32m+[m[32m    const prio  = prioridadePorScore(score);[m
[32m+[m[32m    const etapa = sugerirEtapaParaCarrinho(c, calc.mins);[m
[32m+[m[32m    const itens = Array.isArray(c.produtos)?c.produtos:[];[m
[32m+[m[32m    const prodTxt = itens.slice(0,5).map(it=>String(it?.nome||it?.title||it?.descricao||it?.name||"").trim()).filter(Boolean).join("; ");[m
[32m+[m[32m    const pipeline = c.recuperado?"Recuperado":c.perdido?"Perdido":c.last_etapa_enviada?"Contatado":"Novo";[m
[32m+[m[32m    const dtCriado = c.criado_em ? new Date(c.criado_em).toLocaleString("pt-BR") : "";[m
[32m+[m[32m    const dtRec    = c.recuperado_em ? new Date(c.recuperado_em).toLocaleString("pt-BR") : "";[m
[32m+[m[32m    return [c.cliente_nome||"", c.telefone||"", c.email||"", Number(c.valor||0).toFixed(2).replace(".",","), String(Math.round(score)), prio.label, pipeline, etapa.label||"", prodTxt, dtCriado, dtRec, c.recuperado_pedido_id||"", c.link_finalizacao||"", c.responsavel||""];[m
[32m+[m[32m  });[m
[32m+[m
[32m+[m[32m  const header = ["Nome","Telefone","Email","Valor (R$)","Score","Prioridade","Pipeline","Próxima etapa","Produtos","Criado em","Recuperado em","Pedido recuperado","Link finalização","Responsável"];[m
[32m+[m[32m  const rows = [header, ...list].map(r=>r.map(csvEscape).join(",")).join("\r\n");[m
[32m+[m[32m  downloadCSV(`carrinhos_abandonados_${new Date().toISOString().slice(0,10)}.csv`, rows);[m
[32m+[m[32m  toast(`✓ ${list.length} carrinho${list.length!==1?"s":""} exportado${list.length!==1?"s":""}!`, "success");[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m/* ═══════════════════════════════════════════════════[m
[32m+[m[32m   G — FUNIL DE CONVERSÃO POR ETAPA[m
[32m+[m[32m═══════════════════════════════════════════════════ */[m
[32m+[m[32mlet carrinhosFunilVisible = false;[m
[32m+[m
[32m+[m[32mfunction toggleCarrinhosFunil(){[m
[32m+[m[32m  carrinhosFunilVisible = !carrinhosFunilVisible;[m
[32m+[m[32m  const el = document.getElementById("carrinhos-funil");[m
[32m+[m[32m  const btn = document.getElementById("car-funil-btn");[m
[32m+[m[32m  if(el) el.style.display = carrinhosFunilVisible ? "" : "none";[m
[32m+[m[32m  if(btn) btn.style.fontWeight = carrinhosFunilVisible ? "800" : "";[m
[32m+[m[32m  if(carrinhosFunilVisible) renderCarrinhosFunil();[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mfunction renderCarrinhosFunil(enrichedList){[m
[32m+[m[32m  const el = document.getElementById("carrinhos-funil");[m
[32m+[m[32m  if(!el || !carrinhosFunilVisible) return;[m
[32m+[m[32m  const list = enrichedList || (carrinhosAbandonados||[]).map(c=>{[m
[32m+[m[32m    const n = normalizeCarrinhoAbandonado(c);[m
[32m+[m[32m    const calc = calcularScoreRecuperacaoCarrinho(n, buildClienteLookupParaCarrinhos());[m
[32m+[m[32m    return Object.assign({}, n, {tempo_min: calc.mins});[m
[32m+[m[32m  });[m
[32m+[m
[32m+[m[32m  const total = list.length;[m
[32m+[m[32m  if(!total){ el.innerHTML = '<div style="text-align:center;font-size:12px;color:var(--text-3);padding:12px">Sem dados.</div>'; return; }[m
[32m+[m
[32m+[m[32m  const stages = [[m
[32m+[m[32m    { id:"novo",      label:"Novo",            color:"#64748b", count: list.filter(c=>!c.recuperado&&!c.perdido&&!c.last_etapa_enviada).length },[m
[32m+[m[32m    { id:"ajuda",     label:"Ajuda enviada",   color:"#3b82f6", count: list.filter(c=>!c.recuperado&&!c.perdido&&c.last_etapa_enviada==="ajuda").length },[m
[32m+[m[32m    { id:"link",      label:"Link enviado",    color:"#8b5cf6", count: list.filter(c=>!c.recuperado&&!c.perdido&&c.last_etapa_enviada==="link").length },[m
[32m+[m[32m    { id:"incentivo", label:"Incentivo",       color:"#f59e0b", count: list.filter(c=>!c.recuperado&&!c.perdido&&c.last_etapa_enviada==="incentivo").length },[m
[32m+[m[32m    { id:"recuperado",label:"Recuperado ✓",    color:"#10b981", count: list.filter(c=>c.recuperado).length },[m
[32m+[m[32m    { id:"perdido",   label:"Perdido ✗",       color:"#ef4444", count: list.filter(c=>c.perdido&&!c.recuperado).length },[m
[32m+[m[32m  ];[m
[32m+[m
[32m+[m[32m  const max = Math.max(...stages.map(s=>s.count), 1);[m
[32m+[m[32m  el.innerHTML =[m
[32m+[m[32m    '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:12px">'+[m
[32m+[m[32m      '<span style="font-size:13px;font-weight:800">📊 Funil de conversão</span>'+[m
[32m+[m[32m      '<span style="font-size:11px;color:var(--text-3)">'+ escapeHTML(String(total))+' carrinho'+( total!==1?"s":"")+'</span>'+[m
[32m+[m[32m    '</div>'+[m
[32m+[m[32m    stages.map(s=>{[m
[32m+[m[32m      const pct = total ? Math.round(s.count/total*100) : 0;[m
[32m+[m[32m      const barW = max ? Math.round(s.count/max*100) : 0;[m
[32m+[m[32m      return '<div class="car-funil-row">'+[m
[32m+[m[32m        '<div class="car-funil-label">'+escapeHTML(s.label)+'</div>'+[m
[32m+[m[32m        '<div class="car-funil-bar-wrap"><div class="car-funil-bar-fill" style="width:'+barW+'%;background:'+s.color+'"></div></div>'+[m
[32m+[m[32m        '<div class="car-funil-count">'+escapeHTML(String(s.count))+'</div>'+[m
[32m+[m[32m        '<div class="car-funil-pct">'+escapeHTML(String(pct))+'%</div>'+[m
[32m+[m[32m      '</div>';[m
[32m+[m[32m    }).join("");[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m/* ═══════════════════════════════════════════════════[m
[32m+[m[32m   H — ATRIBUIÇÃO DE RESPONSÁVEL[m
[32m+[m[32m═══════════════════════════════════════════════════ */[m
[32m+[m[32mfunction populateCarRespFilter(){[m
[32m+[m[32m  const sel = document.getElementById("car-responsavel");[m
[32m+[m[32m  if(!sel) return;[m
[32m+[m[32m  const users = loadAccessUsers();[m
[32m+[m[32m  const current = sel.value;[m
[32m+[m[32m  // Manter opção padrão[m
[32m+[m[32m  sel.innerHTML = '<option value="">Responsável</option>';[m
[32m+[m[32m  // Usuários cadastrados[m
[32m+[m[32m  users.forEach(u=>{[m
[32m+[m[32m    const label = String(u.email||"").split("@")[0] || String(u.email||"");[m
[32m+[m[32m    const opt = document.createElement("option");[m
[32m+[m[32m    opt.value = String(u.email||"");[m
[32m+[m[32m    opt.textContent = label;[m
[32m+[m[32m    if(u.email===current) opt.selected = true;[m
[32m+[m[32m    sel.appendChild(opt);[m
[32m+[m[32m  });[m
[32m+[m[32m  // Responsáveis já atribuídos mas não mais cadastrados[m
[32m+[m[32m  const existentes = [...new Set((carrinhosAbandonados||[]).map(c=>String(c.responsavel||"")).filter(Boolean))];[m
[32m+[m[32m  existentes.forEach(r=>{[m
[32m+[m[32m    if(!users.find(u=>u.email===r)){[m
[32m+[m[32m      const opt = document.createElement("option");[m
[32m+[m[32m      opt.value = r; opt.textContent = r;[m
[32m+[m[32m      if(r===current) opt.selected = true;[m
[32m+[m[32m      sel.appendChild(opt);[m
[32m+[m[32m    }[m
[32m+[m[32m  });[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mfunction atribuirResponsavelCarrinho(checkoutId, responsavel){[m
[32m+[m[32m  const cid = String(checkoutId||"");[m
[32m+[m[32m  const idx = (carrinhosAbandonados||[]).findIndex(x=>String(x?.checkout_id||"")===cid);[m
[32m+[m[32m  if(idx<0) return;[m
[32m+[m[32m  carrinhosAbandonados[idx] = Object.assign({}, normalizeCarrinhoAbandonado(carrinhosAbandonados[idx]), { responsavel: responsavel||null });[m
[32m+[m[32m  safeSetItem("crm_carrinhos_abandonados", JSON.stringify(carrinhosAbandonados));[m
[32m+[m[32m  if(supaConnected && supaClient) upsertCarrinhosAbandonadosToSupabase([carrinhosAbandonados[idx]]).catch(()=>{});[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mwindow.exportCarrinhosCSV = exportCarrinhosCSV;[m
[32m+[m[32mwindow.toggleCarrinhosFunil = toggleCarrinhosFunil;[m
[32m+[m[32mwindow.renderCarrinhosFunil = renderCarrinhosFunil;[m
[32m+[m[32mwindow.atribuirResponsavelCarrinho = atribuirResponsavelCarrinho;[m
[32m+[m[32mwindow.populateCarRespFilter = populateCarRespFilter;[m
[32m+[m
[32m+[m[32m/* ═══════════════════════════════════════════════════[m
[32m+[m[32m   F — HISTÓRICO DE INTERAÇÕES POR CARRINHO[m
[32m+[m[32m═══════════════════════════════════════════════════ */[m
[32m+[m[32masync function openCarrinhoHistorico(checkoutId){[m
[32m+[m[32m  const modal  = document.getElementById("car-history-modal");[m
[32m+[m[32m  const body   = document.getElementById("car-hist-body");[m
[32m+[m[32m  const metaEl = document.getElementById("car-hist-meta");[m
[32m+[m[32m  if(!modal || !body) return;[m
[32m+[m
[32m+[m[32m  const cid = String(checkoutId||"");[m
[32m+[m[32m  const c = (carrinhosAbandonados||[]).find(x=>String(x?.checkout_id||"")===cid);[m
[32m+[m[32m  if(!c){ toast("Carrinho não encontrado.","error"); return; }[m
[32m+[m
[32m+[m[32m  const nome  = escapeHTML(c.cliente_nome||"—");[m
[32m+[m[32m  const valor = escapeHTML(fmtBRL(c.valor||0));[m
[32m+[m[32m  if(metaEl) metaEl.textContent = `${c.cliente_nome||"—"} · ${valor}`;[m
[32m+[m[32m  body.innerHTML = '<div style="text-align:center;padding:20px 0;color:var(--text-3);font-size:12px">Carregando...</div>';[m
[32m+[m[32m  modal.style.display = "";[m
[32m+[m
[32m+[m[32m  if(!supaConnected || !supaClient){[m
[32m+[m[32m    body.innerHTML = '<div style="padding:16px 0;text-align:center;font-size:12px;color:var(--text-3)">Conexão com Supabase necessária para ver o histórico.</div>';[m
[32m+[m[32m    return;[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  try{[m
[32m+[m[32m    const customerKey = String(c.email||"").trim().toLowerCase() || rawPhone(c.telefone||"") || "";[m
[32m+[m[32m    let rows = [];[m
[32m+[m
[32m+[m[32m    if(customerKey){[m
[32m+[m[32m      const uuid = await resolveCustomerUuid(customerKey).catch(()=>null);[m
[32m+[m[32m      if(uuid){[m
[32m+[m[32m        const {data} = await supaClient[m
[32m+[m[32m          .from("interactions")[m
[32m+[m[32m          .select("id,type,description,created_at,user_responsible,metadata")[m
[32m+[m[32m          .eq("customer_id", uuid)[m
[32m+[m[32m          .order("created_at", {ascending:false})[m
[32m+[m[32m          .limit(50);[m
[32m+[m[32m        rows = data || [];[m
[32m+[m[32m      }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    // Filtrar para este carrinho especificamente (metadata.checkout_id) ou tipo recovery[m
[32m+[m[32m    const local = rows.filter(r=>[m
[32m+[m[32m      (r.metadata && String(r.metadata.checkout_id||"")===cid) ||[m
[32m+[m[32m      String(r.type||"").includes("carrinho") ||[m
[32m+[m[32m      String(r.description||"").toLowerCase().includes("carrinho")[m
[32m+[m[32m    );[m
[32m+[m[32m    // Mostrar local-specific primeiro, depois o resto se < 5 total[m
[32m+[m[32m    const display = local.length ? local : rows.slice(0,20);[m
[32m+[m
[32m+[m[32m    if(!display.length){[m
[32m+[m[32m      body.innerHTML = '<div style="padding:16px;text-align:center;font-size:12px;color:var(--text-3)">Nenhuma interação registrada. O histórico é criado ao enviar mensagens pelo CRM.</div>';[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    const typeIc